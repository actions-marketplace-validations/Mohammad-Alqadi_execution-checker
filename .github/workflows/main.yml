on: [push]

jobs:
  Demo_job:
    runs-on: ubuntu-latest
    name: Demo
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Demo
        id: Demo
        uses: ./@v2
        with:
          Hash: 'Mona the Octocat'
          Log_status: 'Done'
      - name: Get the output Encrypted
        run: echo "Encrypted ; ${{ steps.Demo.outputs.Encrypted }}"